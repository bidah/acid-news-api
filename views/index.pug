extends ./layout.pug

block content

  .jumbotron
    h1 HN Feed
    h2 We <3 hacker news!

  .list-group
      each item in feed
        a(class='list-group-item' href='/') Boolean(item) ? item.title : 'no title'

